(let
    .location "sticks"
    .place 17
    .say-hi (let (print
        "hello " value ", how is the " subject " in the " location "?\n"))
    .greet (let 
            .subject "rowing"
            (if (= place 2)
               (set .location "UK"))

            (say-hi "sam" "dash" "usa" "hazel"))

    (greet .place 2) 
