(
    .location "sticks"
    .say-hi (print "hello " value ", how is the " subject " in the " location "?\n")
    .greet ( 
            .subject "rowing"
            (if (= place 2)
                    ((print "running")
                        (set .location "UK")))

            (print .data)
            (say-hi "sam" "dash" "usa" "hazel"))

    (greet .place 2))
